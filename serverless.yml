service: meka-boilerplate

plugins:
  - serverless-webpack
  - serverless-offline

provider:
  name: aws
  runtime: nodejs12.x

functions:
  ahBeingRushed:
    name: "meka-boilerplate-ahBeingRushed"
    handler: handler.ahBeingRushed

custom:
  webpack:
    webpackConfig: "webpack.config.js"
    packager: "yarn"
    includeModules: true
  serverless-offline:
    port: 3002
